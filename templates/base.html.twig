<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>
        {% block title %}
            Demo UI
        {% endblock %}
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
        }

        header, footer {
            background: #eee;
            padding: 10px;
        }

        nav a {
            margin-right: 10px;
        }
    </style>
</head>
<body>

<header>
    <h1>
        {% block header %}
            {{ header }}
        {% endblock %}
    </h1>
    <nav>
        <a href="{{ path('home') }}">Home</a>
        <a href="{{ path('password_generate') }}">Generate Password</a>
    </nav>
</header>

<main>
    {% block content %}
        <p>{{ text|default('No content') }}</p>
    {% endblock %}
</main>

<footer>
    {% block footer %}
        &copy; {{ "now"|date("Y") }} All rights reserved.
    {% endblock %}
</footer>

</body>
</html>
